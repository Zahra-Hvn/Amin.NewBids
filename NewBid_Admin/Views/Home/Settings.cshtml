@model Datalayer.ViewModels.vmMain
@{
    ViewBag.Title = "Settings";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<div class="box box-primary">
    <div class="box-header with-border">
        <h3 class="box-title">تنظیمات وب سایت</h3>
    </div>
    <!-- /.box-header -->
    <!-- form start -->
    @*@using (Html.BeginForm("CreateProduct", "Home", FormMethod.Post, new { enctype = "multipart/form-data" }))
    {*@
        <div role="form">
            <div class="box-body">
                <div class="form-group">

                    @using (Html.BeginForm("EditAboutUs","Home",FormMethod.Post))
                    {
                        <div class="form-group">
                            <label>توضیحات درباره ما</label>


                            <!-- tools box -->
                            <!-- /. tools -->
                            <!-- /.box-header -->
                            <div class="box-body pad">
                                <div>
                                    <textarea required name="text" id="mytextarea" style="width: 100%; height: 200px; font-size: 14px; line-height: 18px; border: 1px solid #dddddd; padding: 10px;">@Model.getSettinge.AboutUsCaption</textarea>
                                </div>
                            </div>
                            <div class="box-footer">
                                <button type="submit" class="btn btn-primary">ثبت</button>
                            </div>
                        </div>
                    }




                </div>
                <div class="form-group">

                    @using (Html.BeginForm("EditPrivate", "Home", FormMethod.Post))
                    {

                        <div class="form-group">
                            <label>توضیحات حریم خصوصی</label>


                            <!-- tools box -->
                            <!-- /. tools -->
                            <!-- /.box-header -->
                            <div class="box-body pad">
                                <div>
                                    <textarea required name="text" id="mytextarea" style="width: 100%; height: 200px; font-size: 14px; line-height: 18px; border: 1px solid #dddddd; padding: 10px;">@Model.getSettinge.PrivatreCaption</textarea>
                                </div>
                            </div>
                            <div class="box-footer">
                                <button type="submit" class="btn btn-primary">ثبت</button>
                            </div>
                        </div>
                    }

                    </div>
                <div class="form-group">


                    @using (Html.BeginForm("EditPolicy", "Home", FormMethod.Post))
                    {

                        <div class="form-group">
                            <label>توضیحات قوانین و مقررات </label>


                            <!-- tools box -->
                            <!-- /. tools -->
                            <!-- /.box-header -->
                            <div class="box-body pad">
                                <div>
                                    <textarea  required name="text" id="mytextarea" style="width: 100%; height: 200px; font-size: 14px; line-height: 18px; border: 1px solid #dddddd; padding: 10px;">@Model.getSettinge.PolicyCaption </textarea>
                                </div>
                            </div>
                            <div class="box-footer">
                                <button type="submit" class="btn btn-primary">ثبت</button>
                            </div>
                        </div>
                    }

                        @*<div class="form-group">
                            <label>شماره تماس</label>


                            <!-- tools box -->
                            <!-- /. tools -->
                            <!-- /.box-header -->
                            <div class="box-body pad">
                                <div>
                                    <input type="text" class="form-control" name="tell" value="@Model.getSettinge.PhoneNumber" />
                                </div>
                            </div>
                            <div class="box-footer">
                                <button type="submit" class="btn btn-primary">ثبت</button>
                            </div>
                        </div>*@
                    </div>

            </div>
            <!-- /.box-body -->
           
        </div>
    @*}*@
    @*<img src="D://myimage/photo_2019-01-21_06-07-40.jpg_Product_1007.jpg" alt="Alternate Text" />*@
</div>
<script src="~/Scripts/jquery-3.3.1.min.js"></script>
<script>
    $(document).ready(function () {
        $('#ostan').change(function () {
            var sid = document.getElementById("ostan").value;

            $.post("/Home/GetShahrestan", { sid: sid }, function (data, status) {
                $('#sh').html('');
                for (var i = 0; i < data.length; i++) {
                    var obj = data[i];
                    $('#sh').append("<option id='h' value=" + obj.ID + " selected='selected'> " + obj.ShahrestanTitle + "</option > ");

                }
            });
        });

    });


    $(document).ready(function () {
        $('#ct').change(function () {
            var subid = document.getElementById("ct").value;

            $.post("/Home/getsub", { subid: subid }, function (data, status) {
                $('#mysub').html('');
                for (var i = 0; i < data.length; i++) {
                    var obj = data[i];
                    $('#mysub').append("<option id='cat' value=" + obj.Id + " selected='selected'> " + obj.subTitle + "</option > ");

                }
            });
        });

    });



    $(document).ready(function () {
        $('#sh').change(function () {
            var fid = document.getElementById("sh").value;

            $.post("/Home/getfactory", { fid: fid }, function (data, status) {
                $('#fk').html('');
                for (var i = 0; i < data.length; i++) {
                    var obj = data[i];
                    $('#fk').append("<option id='h' value=" + obj.Id + " selected='selected'> " + obj.FTitle + "</option > ");

                }
            });
        });

    });
</script>
<script src="~/Content/AdminLTE/bower_components/ckeditor/ckeditor.js"></script>
<script src="~/Content/AdminLTE/bower_components/tinymce/tinymce.min.js"></script>

<script>
    tinymce.init({
        selector: 'textarea#mytextarea',
        plugins: 'advlist autolink link lists preview table code pagebreak',
        menubar: false,
        language: 'fa',
        height: 300,
        relative_urls: false,
        toolbar: 'undo redo | removeformat preview code | fontsizeselect bullist numlist | alignleft aligncenter alignright alignjustify | bold italic | pagebreak table link',
    });
    $(function () {
        // Replace the <textarea id="editor1"> with a CKEditor
        // instance, using default configuration.
        CKEDITOR.replace('editor1')
        //bootstrap WYSIHTML5 - text editor
        $('.textarea').wysihtml5()
    })
</script>
